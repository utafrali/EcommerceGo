apiVersion: v1
kind: Service
metadata:
  name: payment
  namespace: ecommerce
  labels:
    app: payment
    app.kubernetes.io/name: payment
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app: payment
  ports:
    - name: http
      port: 8005
      targetPort: 8005
      protocol: TCP
    - name: grpc
      port: 9005
      targetPort: 9005
      protocol: TCP
